/**
 * ActivityCalendar 컴포넌트
 *
 * 활동 달력 프레젠테이션 컴포넌트
 *
 * 주요 기능:
 * - 달력 UI 렌더링
 * - 날짜별 이벤트 마커 표시
 * - 날짜 선택 이벤트 처리
 *
 * Props:
 * - events: { date: string, count: number }[]
 * - selectedDate?: string
 * - onDateSelect: (date: string) => void
 *
 * 라이브러리:
 * - react-native-calendars 또는 커스텀 달력 컴포넌트
 */

// TODO: 컴포넌트 구현 코드 삽입 위치
