/**
 * InterestCloud 컴포넌트
 *
 * 키워드 워드클라우드 컴포넌트
 *
 * 주요 기능:
 * - 키워드를 점수에 따라 다른 크기로 표시
 * - 인터랙티브 클릭 이벤트 (선택사항)
 *
 * Props:
 * - interests: Array<{ keyword: string, rawScore: number }>
 * - onKeywordPress?: (keyword: string) => void
 *
 * 라이브러리:
 * - react-native-wordcloud 또는 커스텀 구현
 */

// TODO: 컴포넌트 구현 코드 삽입 위치
