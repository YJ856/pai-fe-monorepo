/**
 * 부모 프로필/설정 화면
 *
 * 주요 기능:
 * - 프로필 정보 수정
 * - 알림 설정
 * - 보안 설정
 * - 음성 등록/관리
 * - 로그아웃
 *
 * API:
 * - PATCH /api/profiles/:profileId (api/profiles.ts)
 * - POST /api/auth/logout (api/auth.ts)
 *
 * 서브 컴포넌트:
 * - VoiceRegistration (음성 등록)
 */

// TODO: 디자인 코드 삽입 위치
